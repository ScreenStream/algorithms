#include "hash-table.h"

/// MICROSOFT OA
/// https://leetcode.com/discuss/interview-question/525977/

/*
 * Self explanatory question.
 */

int getLargestNum(vector<int>& array) {
    return 0;
}


/*
 * function solution(A) {
  let map = {}
  for (let i = 0; i < A.length; i++) {
    if (!map.hasOwnProperty(A[i])) map[A[i]] = 1
    else map[A[i]]++
  }
  let max = 0
  for (let key in map) {
    if (map[key] == +key && map[key] > max) max = +key
  }
  return max
}
 */