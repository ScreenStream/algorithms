#pragma once

#include <vector>
#include <string>

bool canPartition(std::vector<int>& nums);
int findMaxForm(std::vector<std::string>& strs, int m, int n); /// ones-and-zeroes
int lastStoneWeightII(std::vector<int>& stones);
int change(int amount, std::vector<int>& coins); /// coin-change-2